# =============================================================================
# Customer Churn Analytics Platform - Environment Configuration Template
# =============================================================================
# This template file contains all environment variables needed to run the
# churn analytics platform. Copy this file to .env and fill in your values.
#
# Command: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# MySQL Configuration (Source Database)
# -----------------------------------------------------------------------------
# MySQL is used as the source for structured customer churn data
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_DATABASE=customer_churn

# -----------------------------------------------------------------------------
# MongoDB Configuration (NoSQL Database for Reviews)
# -----------------------------------------------------------------------------
# MongoDB Atlas or local MongoDB instance for customer reviews
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=yourapp
MONGODB_DATABASE=telecom_data
MONGODB_COLLECTION=customer_reviews

# -----------------------------------------------------------------------------
# DuckDB Configuration (Analytical Warehouse)
# -----------------------------------------------------------------------------
# Path to the DuckDB database file (relative to project root)
DUCKDB_PATH=duckdb/churn_warehouse.duckdb

# -----------------------------------------------------------------------------
# Airflow Configuration
# -----------------------------------------------------------------------------
# Enable/disable optional data source ingestion in Airflow DAG
# Set to "1" to enable, leave empty or "0" to disable
MYSQL_ENABLED=0
MONGO_ENABLED=0

# Airflow database connection (for Airflow metadata)
AIRFLOW_DB_URI=sqlite:///airflow/airflow.db

# Airflow webserver configuration
AIRFLOW_WEBSERVER_PORT=8080
AIRFLOW_EXECUTOR=LocalExecutor

# -----------------------------------------------------------------------------
# Dash Application Configuration
# -----------------------------------------------------------------------------
# Port for the Dash analytics dashboard
DASH_HOST=0.0.0.0
DASH_PORT=8050
DASH_DEBUG=True

# -----------------------------------------------------------------------------
# dbt Configuration
# -----------------------------------------------------------------------------
# dbt profile name (should match dbt_project.yml)
DBT_PROFILE=churn_analytics
DBT_TARGET=dev
DBT_THREADS=4

# -----------------------------------------------------------------------------
# Docker Configuration (Optional)
# -----------------------------------------------------------------------------
# If using Docker containers for MySQL/MongoDB
DOCKER_MYSQL_CONTAINER=mysql-churn
DOCKER_MONGO_CONTAINER=mongo-churn

# -----------------------------------------------------------------------------
# Data Paths (Optional Override)
# -----------------------------------------------------------------------------
# Override default data file locations if needed
# CSV_DATA_PATH=sql/data/
# JSON_DATA_PATH=mongo/data/

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
LOG_PATH=logs/

# -----------------------------------------------------------------------------
# Security & Secrets (Production Only)
# -----------------------------------------------------------------------------
# Never commit actual secrets to version control!
# Use environment-specific secret management in production
SECRET_KEY=your_secret_key_here_for_production
